Put all of your history files in beautiful application-appropriate
datestamped records inside the ``~/history`` directory.

Supported languages
==========================
The following applications are supported

* POSIX shell
* ipython
* sqlite3

Parsing history files
==========================
You can also install the historian reader package for reading your
history files.
