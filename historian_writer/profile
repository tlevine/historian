PROFILE_DIR="${HOME}/git/historian/historian_writer/sources"

# Run only in an interactive  shell.
[[ $- != *i* ]] && return

# Run only in a terminal (tty -s)
for file in "${PROFILE_DIR}"/*; do
  . "$file"
done
