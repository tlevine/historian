PROFILE_DIR="${HOME}/.historian/sources"

# Run only in an interactive  shell.
[[ $- != *i* ]] && return

# Run only in a terminal (tty -s) and if it hasn't already been run.
for file in "${PROFILE_DIR}"/*; do
  . "$file"
done
